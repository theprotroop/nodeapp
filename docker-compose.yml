version: '2'
services:
  mynode: 
    build: .
    image: theprotroop-my-node-app:${DOCKER_TAG}
    container_name: theprotroop-my-node-app:${DOCKER_TAG}
    restart: unless-stopped
    ports:
    - "3000:3000"
    #volumes:
      #- /opt/good:/usr/src/app/files
